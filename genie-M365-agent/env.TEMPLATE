################################################################################
# Databricks Genie – M365 Agents
# Environment Configuration (.env)
#
# Notes:
# - This file contains sensitive credentials (client secrets, tokens). 
# - Never commit a filled-in version of this file to Git.
# - Always keep secrets in a secure vault (Azure Key Vault, AWS Secrets Manager, etc.).
# - Use this file only as a local development helper template.
################################################################################

# ------------------------------------------------------------------------------
# Service Connection (Azure AD / Entra ID)
# These settings configure authentication against Microsoft Entra ID 
# for the service principal that the agent will use.
# ------------------------------------------------------------------------------

CONNECTIONS__SERVICE_CONNECTION__SETTINGS__CLIENTID=        # Application (client) ID of the registered Azure AD app
CONNECTIONS__SERVICE_CONNECTION__SETTINGS__CLIENTSECRET=    # Client secret generated for the Azure AD app
CONNECTIONS__SERVICE_CONNECTION__SETTINGS__TENANTID=        # Tenant ID (directory ID) of the Azure AD organization

# ------------------------------------------------------------------------------
# Databricks Connection
# These variables configure OAuth or PAT-based access to the Databricks workspace.
# It is recommended to use OAuth with a Service Principal instead of PAT.
# ------------------------------------------------------------------------------

DATABRICKS_SPACE_ID=       # Specify the space ID of the Genie "spaces" integration
DATABRICKS_HOST=           # Databricks workspace URL, e.g. https://adb-<workspace-id>.<region>.azuredatabricks.net
DATABRICKS_CLIENT_ID=      # Service Principal (app) client ID with Databricks access
DATABRICKS_CLIENT_SECRET=  # Service Principal client secret
DATABRICKS_OAUTH_SCOPES=all-apis sql offline_access   # OAuth scopes requested for Databricks (default: "all-apis sql offline_access")

# ------------------------------------------------------------------------------
# Legacy / Backwards Compatibility
# A Personal Access Token (PAT) can be used if OAuth is not configured. 
# Prefer OAuth via Service Principal for production environments.
# ------------------------------------------------------------------------------

DATABRICKS_TOKEN=          # Databricks Personal Access Token (PAT) – use only if OAuth is unavailable
